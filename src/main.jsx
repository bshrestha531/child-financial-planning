import React from "react";
import ReactDOM from "react-dom/client";
import HeadStartPlanner from "./HeadStartPlanner.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HeadStartPlanner />
  </React.StrictMode>
);
